# Tarefas M0–M6 — fatias verificáveis

Regras: segurança, UI mínima, docs e testes acompanham cada marco.
Reutilizar código M1–M5 de agentes já entregue; não refazer.

- [x] **M0** — `inventory.md`, `conflicts.md`, `tasks.md` (este arquivo). Gate: requisitos ligados a arquivos/testes, incógnitas marcadas.
- [x] **M1** — Fronteira `ModelProvider` (`chat/services/providers/`, só Anthropic; `get_provider` recusa outro com `ProviderUnavailable`) + `AI_PROVIDER`/`OPENAI_ENABLED` em settings + `capabilities.md` (tudo `unknown` sem evidência) + `test_provider_m1.py` (8 testes, inclui transversal sem OpenAI). Gate VERDE em 16/09/2026: 430 passed + 1 skipped (non-e2e), 4 passed (e2e), ruff limpo; UI intocada e preservada.
- [x] **M2** — Runs duráveis (2026-09-16: `RunJournalEvent` + journal/claims/commands, `POST …/runs` 202 idempotente + 409, SSE `events` com cursor e `done` pós-final, cancel idempotente, resume, `run_worker --once` validado offline; 437+4 verdes, ruff limpo; UI com aviso + Interromper).
- [ ] **Dívida M0/M2** — `threat-model.md` + ADRs obrigatórias (§26) antes de M3.
- [x] **M3** — Fronteira `ObjectStore` local + `DB_ENGINE` (snippet Postgres com erro útil); ensaio de migração em destino separado com verificação de integridade; rollback documentado em `migration.md`. Gate: 8 testes verdes (object_store + rehearsal), 445 passed + 1 skipped na suíte; S3/claims PG reais ficam para M4/M6 com preflight.
- [x] **M4** — `OutboxMessage` + dispatcher (`dispatch_outbox --once`) + consumidor idempotente atrás da fronteira `Broker` (lite usa `LocalBroker`, sem infra extra); `RetrievalIndex` com `local` (FTS5) e `elastic` (falha útil sem cluster). Gate: 5 testes verdes (upload→outbox→ready, falha publish/marca, duplicata, comando, índice); RabbitMQ/Celery/Elastic reais ficam para o homelab (M6 preflight).
- [x] **M5** — Agentes/pensamento/cache/multimodal reutilizados sobre o runtime durável; `agents_eval` 24/24 (individual 8/8, equipe 16/16) sem OpenAI; revalidação real de citações filho→pai (`agents/evidence.py`, só chunk autorizado vira citação); `evaluation-plan.md` criado. Gate: 453 passed + 1 skipped na suíte total, ruff limpo.
- [ ] **M6** — Fatia local pronta: `manage.py preflight` somente-leitura + `GET /api/health` (4 testes), esqueleto OIDC validável (`services/identity.py`), docs `spec/architecture/verification.md` + 12 ADRs. Gate pago `test_live.py` 2/2 verde (22/09/2026; salt por execução; anomalia transitória de cache documentada em `verification.md`). Cluster liberado em 22/09/2026 (kubectl do Mac → k3s `k8s1`): Gateway/HTTPRoutes/Authentik/MinIO/Grafana no ar, **mas nó sem memória** (requests ≈ 14,2Gi > ~10Gi; `rabbitmq-0`, `elastic-es-default-0`, `redis-master-0`, `loki-0` pendentes) — perfil homelab degradado (§21). Decisão do usuário em 22/09: só documentar e parar. **Retomada 22/09 (k3s saudável, 57Gi, 0 Pending)**: (1) memória OK ✓; (2) `preflight --cluster` pronto + 10/10 ao vivo ✓ (`tests/integration/test_cluster_preflight_m6.py`, 6 testes); (3) `deploy/` pronto, namespace `chat-local` criado, resto validado `dry-run=server` ✓ (sem segredos; imagem via Harbor). **Falta**: (4) operador publica imagem + cria Secret (`deploy/README.md` §2) → `apply` autorizado → smoke/E2E + OIDC contra Authentik. Gate: deploy interno observado; matriz §21 demonstrada.

**Backlog (fora desta entrega)**: extensão OpenAI mediante solicitação futura. Não implementar em M0–M6.
