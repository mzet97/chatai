# M6 — Falhas, recuperação, testes integrados, docs didáticas (incremento SDD)

## Cobertura nova (o resto já veio em M1–M5)
1. Troca de catálogo/revogação entre pausa e retomada → `revoked`, sem efeito.
2. Isolamento entre usuários (MCP concedido × não concedido; PUT valida).
3. Injeção em descrição/resultado não autoriza escrita nem muda system.
4. Limites pós-retomada (contadores persistem; esgotado → erro, sem call extra).
5. Redução de contexto: pares intactos, sem blocos protocolares no histórico.
6. ASGI real: rota de tools exige login de ponta a ponta.
7. Aceite: MCP demo + calc + recusa sem escrita + aprova nova + persiste
   após reinício (SQLite arquivo) + modo completo.

## Docs
- `docs/tools-mcp/` + evidências; README/CLAUDE.md: tool calling × MCP,
  blocos, autorização, dados por destino, continuidade, limitações e o que
  NÃO foi executado (Keychain ao vivo, API real paga, OAuth ao vivo).
